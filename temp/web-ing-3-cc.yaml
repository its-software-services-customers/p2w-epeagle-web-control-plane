apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-codecheck3-ing
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/ingress.allow-http: "true" #Wordpress will redirect to https internally
spec:
  tls:
    - hosts:
      - {{ .Values.DomainWeb3 }}
      - www.{{ .Values.DomainWeb3 }}
      secretName: web-cert-3
  rules:
    - host: {{ .Values.DomainWeb3 }}
      http:
        paths:
          - path: /epeagle
            pathType: Prefix
            backend:
              service:
                name: codecheck-external-ip
                port:
                  number: 80
  
    - host: www.{{ .Values.DomainWeb3 }}
      http:
        paths:
          - path: /epeagle
            pathType: Prefix
            backend:
              service:
                name: codecheck-external-ip
                port:
                  number: 80
